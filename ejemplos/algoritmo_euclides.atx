// Para poder usar este algoritmo_euclides,
// en el main.js hay que poner await readInput(2)
potion panoramix() -> intix {
    intix M;
    intix N;

    tabellae(M);
    tabellae(N);

    whilix (!(M == N)) {
        if (M <= N) {
            N = N - M;
        } else {
            M = M - N;
        }
    }

    stilus(M);

    return 0;
}
